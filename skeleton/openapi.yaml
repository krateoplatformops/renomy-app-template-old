openapi: 3.0.1
info:
  title: ReNoMy Sample App
  description: ReNoMy Sample App Template
  version: "0.1"
servers:
  - url: https://${{values.apiUrl}}
paths:
  /logs:
    get:
      description: Auto generated using Swagger Inspector
      responses:
        "200":
          description: Auto generated using Swagger Inspector
          content:
            application/json; charset=utf-8:
              schema:
                type: string
              examples: {}
      servers:
        - url: https://${{values.apiUrl}}
    servers:
      - url: https://${{values.apiUrl}}
  /todo:
    get:
      description: Auto generated using Swagger Inspector
      responses:
        default:
          description: Auto generated using Swagger Inspector
      servers:
        - url: https://${{values.apiUrl}}
    post:
      description: Auto generated using Swagger Inspector
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
            examples:
              "0":
                value: { "message": "test swagger" }
      responses:
        "200":
          description: Auto generated using Swagger Inspector
          content:
            application/json; charset=utf-8:
              schema:
                type: string
              examples: {}
      servers:
        - url: https://${{values.apiUrl}}
    delete:
      description: Auto generated using Swagger Inspector
      parameters:
        - name: id
          in: query
          schema:
            type: string
          example: "4"
      responses:
        "200":
          description: Auto generated using Swagger Inspector
      servers:
        - url: https://${{values.apiUrl}}
    servers:
      - url: https://${{values.apiUrl}}
